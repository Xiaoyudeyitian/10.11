#include<stdio.h>
int main()
{
	int a,b,c,max;
	printf("任意输入三个数：");
	scanf_s("%d%d%d", & a, & b, & c);
	if (a > b)
		max = a;
	else
		max = b;
    if (c>max)
		max = c;
	printf("%d", max);
	return 0 ;
}
